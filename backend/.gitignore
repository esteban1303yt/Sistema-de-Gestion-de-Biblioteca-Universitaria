#Dependencias a ignorar
node_modules/
package-lock.json

#Archivos de entorno
.env.local
.env.*.local

#logs (son archivos d registro generados por el sistema o cualquier otro ebn .log)
logs
*.logs
npm-debug.log*
yarn-debug.log*
yarn-eror.log*

#Directorios del runtime (ignora las carpetas generadas automaticamente por el sistema)
dist/
build/
tmp/
temp/

#Archivos del sistema (archivos generados por el sistema operativo)
.Ds_Store
Thumbs.db

#Directorios y archivos de IDLE / editores
.idea/
.vscode/
*.swp
*.swo
*.swn
*.bak

#Archivos del debug (ignora archivos usados para depurar errores)
.debug
debug.log

#Archivos de cobertura (ignora resultados de herramientas de cobertura de pruebas)
coverage/

#Archivos de uploads (excepto el directorio vacio)
uploads/*
!uploads/.gitkeep

#Otros (ignora archivos de procesos y archivos de bloqueo o semilla usados en ejecucion )
*.pid
*.seed
*.pid.lock